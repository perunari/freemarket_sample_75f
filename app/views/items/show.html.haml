= render 'layouts/searchheader'

%main
  .contents

    .product
      %h1.product__name
        = @item.name
        -# = @item.name[:id]
        -# product3
      .product__images
        .product__images__main-image
          = image_tag '/product_image_main.png', width: '560', height: '350'
        %ul.product__images__sub-images
          %li.product__images__sub-images__image
            = image_tag '/product_image_sub1.png', width: '140', height: '90'
          %li.product__images__sub-images__image
            = image_tag '/product_image_sub2.png', width: '140', height: '90'
          %li.product__images__sub-images__image
            = image_tag '/product_image_sub3.png', width: '140', height: '90'

      .product__price-info
        .product__price-info__price
          = "#{(@item.price * @tax).to_i}円"
          -# = "#{(@item.price[:id] * @tax).to_i}円"
        .product__price-info__include
          .product__price-info__include--tax
            (税込)
          .product__price-info__include--postage
            = @item.postage_payment
            -# = @item.postage_payment[:id]
      
      .product__description
        = @item.description
      %table
        %tr
          %th
            出品者
            %td
              = @item.user.nickname
        %tr
          %th
            カテゴリー
            %td
              %ul.product__categories
                %li
                  = link_to '#', class: 'product__category' do
                    ベビー・キッズ
                %li
                  = link_to '#', class: 'product__category' do
                    ベビー服(男女兼用) ~95cm
                %li
                  = link_to '#', class: 'product__category' do
                    アウター
        %tr
          %th
            ブランド
            %td
              = @item.brand.name
        %tr
          %th
            商品のサイズ
            %td
              
        %tr
          %th
            商品の状態
            %td
              = @item.condition
        %tr
          %th
            配送料の負担
            %td
              = @item.postage_payment
        %tr
          %th
            発送元の地域
            %td
              = @item.ship_from
        %tr
          %th
            発送日の目安
            %td
              = @item.preparation
      .product__bottom
        %button.product__bottom__like-btn
          = icon('fas', 'star')
          お気に入り 0
        = link_to '#', class: 'product__bottom__report-btn' do
          = icon('fab', 'font-awesome-flag')
          不適切な商品の通報







    -# if user_signed_in? && current_user.id == @post.user_id

    .edit-destroy
      %button.edit-destroy__edit-btn
        商品の編集
      %button.edit-destroy__destroy-btn
        商品の削除









    .comment
      %form.comment-form
        %textarea.comment-form__textarea{rows: '5'}
        .comment-form__notice
          相手のことを考え丁寧なコメントを心がけましょう。<br>不快な言葉遣いなどは利用制限や退会処分となることがあります。
        %button.comment-form__submit-btn
          = icon('fas', 'comment')
          コメントする

    .previous-and-next
      = link_to '#', class: 'previous-product' do
        = icon('fas', 'chevron-left', class: 'icon--chevron')
        前の商品
      = link_to '#', class: 'next-product' do
        後ろの商品
        = icon('fas', 'chevron-right', class: 'icon--chevron')

    = link_to '#', class: 'same-category-products' do
      ベビー・キッズをもっと見る

= render 'layouts/camerabtn'
= render 'layouts/richfooter'